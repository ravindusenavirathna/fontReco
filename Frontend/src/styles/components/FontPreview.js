import styled from "styled-components";

export const FontPreviewContainerHeading = styled.h2`
  font-size: 1.5rem;
  margin-bottom: 1rem;
`;

export const FontPreviewSampleText = styled.p`
  font-size: 1.25rem;
`;

export const FontPreviewDownloadLink = styled.a`
  display: inline-block;
  margin-top: 1rem;
  color: #007bff;
  text-decoration: none;

  &:hover {
    text-decoration: underline;
  }
`;
